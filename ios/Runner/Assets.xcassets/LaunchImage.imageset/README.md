# Launch Screen Assets

These launch images are derived from Ghosteye's shared master icon artwork.

To rebuild them after changing the icon source, run:

```bash
dart run tool/generate_brand_assets.dart \
  --source=assets/branding/ghosteye-icon-source-ai.png
```

That command also refreshes the Android launcher icons, Android launch logo, and web icons so the platforms stay aligned.
