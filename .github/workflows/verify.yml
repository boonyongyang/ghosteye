name: Verify

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  flutter:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.4"
          channel: stable
          cache: true

      - name: Install dependencies
        run: make bootstrap

      - name: Audit docs for absolute local links
        run: make docs-audit

      - name: Verify
        run: make verify
